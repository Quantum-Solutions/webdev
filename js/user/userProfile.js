$(document).ready(function() {
    
    try {
        const response = await ;

        if(response.ok){
            //Body from the API
           var userDetails;

           $('#firstName').val = userDetails.firstName;
        }


    } catch (error) {
        
    }
  });